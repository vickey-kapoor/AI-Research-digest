name: Daily Applied AI Dev Digest

# Sends daily AI research digest to Telegram

on:
  schedule:
    # Daily digest. 16:00 UTC = 11:00 AM CDT / 10:00 AM CST.
    # Minute 17 rather than 00: GitHub's shared schedulers are most congested
    # on the hour, and this workflow has been delivered up to 9 hours late.
    - cron: '17 16 * * *'
    # Weekly roundup, Sundays. 17:00 UTC = 12:00 PM CDT / 11:00 AM CST.
    - cron: '17 17 * * 0'
  workflow_dispatch: # Allow manual trigger

permissions:
  contents: write

jobs:
  send-news:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.2.2

      - name: Validate secrets
        run: |
          missing=""
          [ -z "${{ secrets.OPENAI_API_KEY }}" ] && missing="$missing OPENAI_API_KEY"
          [ -z "${{ secrets.TELEGRAM_BOT_TOKEN }}" ] && missing="$missing TELEGRAM_BOT_TOKEN"
          [ -z "${{ secrets.TELEGRAM_CHAT_ID }}" ] && missing="$missing TELEGRAM_CHAT_ID"
          [ -z "${{ secrets.KV_REST_API_URL }}" ] && missing="$missing KV_REST_API_URL"
          [ -z "${{ secrets.KV_REST_API_TOKEN }}" ] && missing="$missing KV_REST_API_TOKEN"
          if [ -n "$missing" ]; then
            echo "::error::Missing required secrets:$missing"
            exit 1
          fi

      - name: Set up Python
        uses: actions/setup-python@v5.6.0
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Applied AI dev digest
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_RUN_ID: ${{ github.run_id }}
          KV_REST_API_URL: ${{ secrets.KV_REST_API_URL }}
          KV_REST_API_TOKEN: ${{ secrets.KV_REST_API_TOKEN }}
          # Passed as env rather than interpolated into the script body:
          # ${{ }} inside run: splices text straight into the shell.
          TRIGGER_SCHEDULE: ${{ github.event.schedule }}
        run: |
          # Route by which schedule fired. Previously both crons fell through
          # to main.py, so every Sunday sent two daily digests an hour apart
          # on top of the weekly roundup. TRIGGER_SCHEDULE is empty for a
          # manual dispatch, which falls through to the daily digest.
          if [ "$TRIGGER_SCHEDULE" = "17 17 * * 0" ]; then
            echo "Weekly schedule — sending the roundup only"
            python weekly_digest.py
          else
            echo "Daily schedule — sending the daily digest"
            python main.py
          fi

      - name: Commit and push reports and data
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add reports/ data/
          # Include the UTC time: scheduler drift can put two runs on one date,
          # and a date-only message makes them indistinguishable in the log.
          git diff --staged --quiet || git commit -m "Add daily research digest for $(date -u +'%d-%b-%Y %H:%M') UTC"
          git push

      - name: Annotate on failure
        if: failure()
        run: |
          echo "::error::Applied AI Dev Digest pipeline failed. Check the logs above for details."

